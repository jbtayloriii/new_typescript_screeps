# Scripts
One-off scripts for testing out behavior outside of the screeps environment.

These should be executed from the root directory.

`auth_codes.json`: A JSON object containing auth codes for interacting with the Screeps HTTP endpoints
`base_mapping.js`: Performs base planning for a given room
